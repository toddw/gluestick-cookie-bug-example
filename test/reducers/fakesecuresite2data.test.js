import reducer from "reducers/fakesecuresite2data";

describe("reducers/fakesecuresite2data", () => {
  it("returns the initial state", () => {
    const state = void 0;
    expect(
      reducer(state, {})
    ).toBeNull();
  });
});
