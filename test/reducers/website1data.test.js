import reducer from "reducers/website1data";

describe("reducers/website1data", () => {
  it("returns the initial state", () => {
    const state = void 0;
    expect(
      reducer(state, {})
    ).toBeNull();
  });
});
